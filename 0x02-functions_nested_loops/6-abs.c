#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @integer: The value to compute
 *
 * Return: _abs(int)
 */
int _abs(int integer)
{
	if (integer > 0)
		return (integer);
	else
		return (integer * -1);
}
